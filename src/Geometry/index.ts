import { resample } from './resample';
import { createPolygon } from './createPolygon';
import { getStraightSkeleton } from './getStraightSkeleton';
import { getBCD } from './getBCD';

export const Geometry = {
  createPolygon,
  getStraightSkeleton,
  getBCD,
  resample,
};
